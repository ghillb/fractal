name: Evolve Agent

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  evolve:
    uses: ./.github/workflows/evolve-task.yml
    secrets: inherit
    with:
      baseline_ref: ${{ github.ref_name }}
      goal: Become an entity that is ever more capable while improving safely.
      candidate_name: primary-${{ github.run_id }}-${{ github.run_attempt }}
